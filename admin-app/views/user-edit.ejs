<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <% if (buildType === 'gulp') { %>
        <link href="/dist-gulp/css/main.min.css" rel="stylesheet">
    <% } else { %>
        <link href="/dist-webpack/css/main.min.css" rel="stylesheet">
    <% } %>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/?build=<%= buildType %>">
                <i class="fas fa-users me-2"></i>Социальная сеть
            </a>
            <div class="navbar-nav ms-auto">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-tools me-1"></i>Сборка: <%= buildType %>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="?build=gulp">Gulp сборка</a></li>
                        <li><a class="dropdown-item" href="?build=webpack">Webpack сборка</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <a href="/?build=<%= buildType %>" class="btn btn-secondary me-3">
                    <i class="fas fa-arrow-left me-1"></i>Назад
                </a>
                <h1 class="d-inline-block">
                    <i class="fas fa-edit me-2"></i><%= title %>
                </h1>
            </div>
        </div>

        <div class="card edit-form">
            <div class="card-body">
                <form id="editUserForm" action="/users/<%= user.id %>/update?build=<%= buildType %>" method="POST">
                    <div class="row mb-3">
                        <div class="col-md-3 text-center">
                            <img src="<%= user.avatar || '/public/images/default-avatar.png' %>" 
                                 alt="<%= user.name %>" 
                                 class="user-avatar mb-3"
                                 onerror="this.src='/public/images/default-avatar.png'">
                            <p class="text-muted small">ID: <%= user.id %></p>
                        </div>
                        <div class="col-md-9">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="name" class="form-label">ФИО</label>
                                    <input type="text" class="form-control" id="name" name="name" 
                                           value="<%= user.name %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="email" class="form-label">Email</label>
                                    <input type="email" class="form-control" id="email" name="email" 
                                           value="<%= user.email %>" required>
                                </div>
                            </div>
                            
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="role" class="form-label">Роль</label>
                                    <select class="form-select" id="role" name="role" required>
                                        <option value="user" <%= user.role === 'user' ? 'selected' : '' %>>Пользователь</option>
                                        <option value="moderator" <%= user.role === 'moderator' ? 'selected' : '' %>>Модератор</option>
                                        <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>Администратор</option>
                                    </select>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="status" class="form-label">Статус</label>
                                    <select class="form-select" id="status" name="status" required>
                                        <option value="pending" <%= user.status === 'pending' ? 'selected' : '' %>>Не подтверждён</option>
                                        <option value="active" <%= user.status === 'active' ? 'selected' : '' %>>Активный</option>
                                        <option value="blocked" <%= user.status === 'blocked' ? 'selected' : '' %>>Заблокирован</option>
                                    </select>
                                </div>
                            </div>
                            
                            <!-- ИЗМЕНЕНО: Заменяем статический текст на редактируемое поле даты -->
                            <div class="mb-3">
                                <label for="birthDate" class="form-label">Дата рождения</label>
                                <input type="date" class="form-control" id="birthDate" name="birthDate" 
                                       value="<%= new Date(user.birthDate).toISOString().split('T')[0] %>" required>
                                <div class="form-text text-muted">
                                    <i class="fas fa-birthday-cake me-1"></i>
                                    Текущая дата: <%= new Date(user.birthDate).toLocaleDateString('ru-RU') %>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="d-flex justify-content-between">
                        <a href="/?build=<%= buildType %>" class="btn btn-secondary">Отмена</a>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-1"></i>Сохранить изменения
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <% if (buildType === 'gulp') { %>
        <script src="/dist-gulp/js/main.min.js"></script>
    <% } else { %>
        <script src="/dist-webpack/js/main.min.js"></script>
    <% } %>
</body>
</html>