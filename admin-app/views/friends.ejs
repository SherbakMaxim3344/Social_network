<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <% if (buildType === 'gulp') { %>
        <link href="/dist-gulp/css/main.min.css" rel="stylesheet">
    <% } else { %>
        <link href="/dist-webpack/css/main.min.css" rel="stylesheet">
    <% } %>
</head>
<body>
    <!-- Навигация -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/?build=<%= buildType %>">
                <i class="fas fa-users me-2"></i>Социальная сеть
            </a>
            
            <div class="navbar-nav ms-auto">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" 
                       data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-tools me-2"></i>
                        Сборка: <span class="fw-bold ms-1"><%= buildType %></span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark">
                        <li><a class="dropdown-item" href="?build=gulp">
                            <i class="fas fa-cube me-2"></i>Gulp сборка
                        </a></li>
                        <li><a class="dropdown-item" href="?build=webpack">
                            <i class="fas fa-cogs me-2"></i>Webpack сборка
                        </a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <!-- Заголовок -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <a href="/?build=<%= buildType %>" class="btn btn-secondary me-3">
                    <i class="fas fa-arrow-left me-1"></i>Назад
                </a>
                <h1 class="d-inline-block text-white">
                    <i class="fas fa-user-friends me-2"></i><%= title %>
                </h1>
            </div>
            <span class="badge bg-info fs-6 px-3 py-2">
                Друзей: <%= friends.length %>
            </span>
        </div>

        <!-- Карточка текущего пользователя -->
        <div class="card friend-card mb-4">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <img src="<%= user.avatar || '/public/images/default-avatar.png' %>" 
                         alt="<%= user.name %>" 
                         class="friend-avatar me-3"
                         onerror="this.src='/public/images/default-avatar.png'">
                    <div>
                        <h5 class="card-title mb-1"><%= user.name %></h5>
                        <p class="card-text text-muted mb-0"><%= user.email %></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Список друзей -->
        <div class="row g-4">
            <% if (friends.length > 0) { %>
                <% friends.forEach(friend => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card friend-card h-100">
                            <div class="card-body">
                                <div class="d-flex align-items-center">
                                    <img src="<%= friend.avatar || '/public/images/default-avatar.png' %>" 
                                         alt="<%= friend.name %>" 
                                         class="friend-avatar me-3"
                                         onerror="this.src='/public/images/default-avatar.png'">
                                    <div class="flex-grow-1">
                                        <h6 class="card-title mb-1"><%= friend.name %></h6>
                                        <p class="card-text text-muted small mb-1"><%= friend.email %></p>
                                        <span class="status-badge <%= friend.status %>">
                                            <%= friend.status === 'active' ? 'Активный' : 
                                               friend.status === 'blocked' ? 'Заблокирован' : 'Не подтвержден' %>
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12">
                    <div class="empty-state">
                        <i class="fas fa-user-friends fa-3x text-muted mb-3"></i>
                        <h3 class="text-muted">Нет друзей</h3>
                        <p class="text-muted">У этого пользователя пока нет друзей</p>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <% if (buildType === 'gulp') { %>
        <script src="/dist-gulp/js/main.min.js"></script>
    <% } else { %>
        <script src="/dist-webpack/js/main.min.js"></script>
    <% } %>
</body>
</html>