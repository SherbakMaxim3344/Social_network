<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <link href="/css/profile.css" rel="stylesheet">
    <style>
        .profile-stats {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .friend-preview {
            transition: transform 0.3s ease;
        }
        .friend-preview:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/news/<%= user.id %>">Социальная сеть</a>
            <div class="navbar-nav ms-auto">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown">
                        <img src="<%= user.avatar || defaultAvatar %>" class="user-avatar me-2">
                        <%= user.name %>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="/profile/<%= user.id %>">👤 Мой профиль</a></li>
                        <li><a class="dropdown-item" href="/friends/<%= user.id %>">🤝 Друзья</a></li>
                        <li><a class="dropdown-item" href="/news/<%= user.id %>">📰 Лента новостей</a></li>
                        <% if (user.role === 'admin') { %>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="https://localhost:3444" target="_blank">⚙️ Админ панель</a></li>
                        <% } %>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item text-danger" href="/">🚪 Выйти</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mt-4 fade-in">
        <div class="row">
            <div class="col-md-8 mx-auto">
                <div class="card">
                    <div class="card-body text-center">
                        <img src="<%= user.avatar || defaultAvatar %>" 
                             class="avatar-large mb-3"
                             alt="<%= user.name %>"
                             id="profile-avatar">
                        <h2 id="profile-name"><%= user.name %></h2>
                        <p class="text-muted" id="profile-email"><%= user.email %></p>
                        
                        <div class="profile-stats mt-4">
                            <div class="row">
                                <div class="col-4">
                                    <h4 id="friends-count"><%= friendsCount %></h4>
                                    <small>Друзей</small>
                                </div>
                                <div class="col-4">
                                    <h4>
                                        <% if (user.status === 'active') { %>
                                            <span class="badge bg-success">Активен</span>
                                        <% } else if (user.status === 'pending') { %>
                                            <span class="badge bg-warning">Ожидание</span>
                                        <% } else { %>
                                            <span class="badge bg-danger">Заблокирован</span>
                                        <% } %>
                                    </h4>
                                    <small>Статус</small>
                                </div>
                                <div class="col-4">
                                    <h4>
                                        <span class="badge bg-info">
                                            <% if (user.role === 'admin') { %>
                                                Админ
                                            <% } else if (user.role === 'moderator') { %>
                                                Модератор
                                            <% } else { %>
                                                Пользователь
                                            <% } %>
                                        </span>
                                    </h4>
                                    <small>Роль</small>
                                </div>
                            </div>
                        </div>

                        <div class="row mt-4">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-header">
                                        <h5 class="mb-0">👥 Последние друзья</h5>
                                    </div>
                                    <div class="card-body">
                                        <% if (friends.length === 0) { %>
                                            <p class="text-muted text-center">Пока нет друзей</p>
                                        <% } else { %>
                                            <div class="row" id="friends-preview">
                                                <% friends.slice(0, 3).forEach(friend => { %>
                                                    <div class="col-md-4 mb-3">
                                                        <div class="friend-preview text-center">
                                                            <img src="<%= friend.avatar || defaultAvatar %>" 
                                                                 class="avatar-medium mb-2">
                                                            <h6 class="mb-1"><%= friend.name %></h6>
                                                            <small class="text-muted d-block"><%= friend.email %></small>
                                                        </div>
                                                    </div>
                                                <% }); %>
                                            </div>
                                            <div class="text-center mt-3">
                                                <a href="/friends/<%= user.id %>" class="btn btn-primary">
                                                    Посмотреть всех друзей (<%= friends.length %>)
                                                </a>
                                            </div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Анимация аватара при загрузке
            const avatar = document.getElementById('profile-avatar');
            if (avatar) {
                avatar.style.opacity = '0';
                avatar.style.transform = 'scale(0.8)';
                
                setTimeout(() => {
                    avatar.style.transition = 'all 0.5s ease';
                    avatar.style.opacity = '1';
                    avatar.style.transform = 'scale(1)';
                }, 100);
            }
            
            // Анимация появления друзей
            const friendPreviews = document.querySelectorAll('.friend-preview');
            friendPreviews.forEach((preview, index) => {
                preview.style.opacity = '0';
                preview.style.transform = 'translateY(20px)';
                
                setTimeout(() => {
                    preview.style.transition = 'all 0.5s ease';
                    preview.style.opacity = '1';
                    preview.style.transform = 'translateY(0)';
                }, 200 + (index * 100));
            });
        });
    </script>
</body>
</html>